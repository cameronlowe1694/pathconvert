services:
  - type: web
    name: pathconvert
    runtime: node
    buildCommand: npm install && npm run build:frontend
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
