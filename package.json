{
  "name": "pathconvert",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "cd server && npm install && npx prisma generate --schema=../prisma/schema.prisma && npx prisma db push --schema=../prisma/schema.prisma --accept-data-loss",
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm start",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "docker-start": "npm run setup && npm run build && npm run start",
    "prisma": "npx prisma --schema=prisma/schema.prisma",
    "deploy": "shopify app deploy"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
